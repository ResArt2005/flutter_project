name: media_gallery_backend
description: Dart shelf server for media gallery
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  postgres: ^2.4.0
  dotenv: ^3.0.0
  shelf_static: ^1.1.0
  shelf_cors_headers: ^0.1.5
  shelf_multipart: ^1.0.0
  path: ^1.8.0
  crypto: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0